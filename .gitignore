node_modules/
.next/
out/
# Keep env example for docs; never commit real keys
.env
.env.*
!.env.example
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
# Video registry file (when ENABLE_VIDEO_REGISTRY=true)
data/
# Test videos at project root (app does not use these)
/testvideo.mp4
